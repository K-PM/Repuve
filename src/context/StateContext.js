import { createContext } from "react";

/*identificador que va apuntar a la referencia del contexto (al contexto)*/
const StateContext = createContext({})

/*Exportacion del archivo*/
export default StateContext;